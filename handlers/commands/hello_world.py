from telebot import TeleBot
from telebot.types import Message


def bot_hello_world(bot: TeleBot, message: Message) -> None:
    """
    Обрабатывает команду /hello_world, отправляя приветственное сообщение пользователю.

    Args:
        bot: Экземпляр телеграм-бота
        message: Входящее сообщение от пользователя

    Returns:
        None: Функция не возвращает значения, только отправляет сообщение

    Example:
        При получении команды "/hello_world" от пользователя с именем "Иван",
        бот ответит: "Привет, Иван! 👋"
    """
    bot.reply_to(message, f"Привет, {message.from_user.first_name}! 👋")
